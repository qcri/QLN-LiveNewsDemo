# propaganda worker - proppy

##setting up

use PyEnv and PipEnv to manage python environment

```console
export PYENV_ROOT=$(pyenv root)
pipenv install
```

## test

```console
PYTHONPATH=.. pipenv run nosetests
```

